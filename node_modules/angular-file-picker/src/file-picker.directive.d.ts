import { ElementRef, EventEmitter, OnInit, Renderer } from '@angular/core';
import { PickedFile } from './picked-file';
export declare class FilePickerDirective implements OnInit {
    private el;
    private renderer;
    accept: string;
    filePick: EventEmitter<PickedFile>;
    private input;
    constructor(el: ElementRef, renderer: Renderer);
    ngOnInit(): void;
    browse(): void;
    private readFile(file);
}
